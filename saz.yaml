# server:
#   base_path: "/saz"

database:
  postgres:
    db_datasource: "postgres://postgres@localhost:5432/postgres?sslmode=disable"
    db_type: "pgx"

store:
  postgres:
    table_prefix: "saz_"
    db_datasource: "postgres://postgres@localhost:5432/postgres?sslmode=disable"
    db_type: "pgx"
    migrate:
      db_datasource: "postgres://postgres@localhost:5432/postgres?sslmode=disable"
      db_schema: "public"

# process:
#   retention: "14d" # remove processes older than 14 days
#   interval: "1d"    # run cleanup every day
